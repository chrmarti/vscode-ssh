{
    "name": "ssh",
    "displayName": "ssh",
    "description": "ssh integration",
    "version": "0.0.1",
    "publisher": "chrmarti",
    "engines": {
        "vscode": "^1.12.0"
    },
    "categories": [
        "Languages"
    ],
    "contributes": {
        "languages": [{
            "id": "ssh_config",
            "aliases": ["ssh config", "ssh_config"],
            "filenames": ["ssh.config"],
            "configuration": "./language-configuration.json"
        }],
        "grammars": [{
            "language": "ssh_config",
            "scopeName": "config.ssh",
            "path": "./syntaxes/ssh_config.tmLanguage.json"
        }]
    }
}